# database init, supports mysql too
database=mysql
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost/hotel}
spring.datasource.username=${MYSQL_USER:hotel}
spring.datasource.password=${MYSQL_PASS:hotel}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
